server:
  port: 9001

spring:
  application:
   name:sk-test
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://120.77.214.98:3307/yingxin?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: feng1234

#redis 配置
  redis:
    host: 120.77.214.98
    port: 8000
    password: feng1234

  #邮箱配置
  mail:
    host: smtp.126.com #发送邮件服务器
    username: samkuang@126.com #发送邮件的邮箱地址
    password: IXJKMEDCUUQQWSGO #客户端授权码，不是邮箱密码,网易的是自己设置的
    from: samkuang@126.com # 发送邮件的地址，和上面username一致
    properties.mail.smtp.starttls.enable: true
    properties.mail.smtp.starttls.required: true
    properties.mail.smtp.ssl.enable: true
    default-encoding: utf-8

mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

#设置日志级别,ERROR/WARN/INFO/DEBUG,默认是INFO以上才显示
logging:
  level:
    root: INFO